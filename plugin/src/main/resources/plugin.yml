name: ${rootProject.name}
version: ${rootProject.verison}
api-version: 1.12
main: net.projecttl.reloader.ReloaderPlugin

commands:
  reloader:
    description: Reloader command
    usage: /rl <argument>
    aliases: [rl]